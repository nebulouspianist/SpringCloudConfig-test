spring:
  application:
    name: config-client
  
server:
  port: 7777

user:
  role: Keye

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
